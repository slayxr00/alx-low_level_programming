#include <stdio.h>
/**
 * main -A program that prints a line usin the printf f
 * Return: 0 (Success)
 */

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n")
			return (0);
			}
